# For Streamlit deployment (app.py)
streamlit==1.28.0
pandas==2.1.1
plotly==5.17.0
numpy==1.24.3

# For Vercel deployment (minimal requirements)
# Vercel uses serverless functions, so we only need basic Python

